{
  "devDependencies": {
    "medium-zoom": "^1.1.0",
    "vitepress": "^1.6.4"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "fetch-latest-release": "node scripts/fetch-latest-release.js",
    "docs:prebuild": "npm run fetch-latest-release"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
